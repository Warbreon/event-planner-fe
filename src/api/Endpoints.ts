const server = 'http://localhost:8080/api';
export const authentication = `${server}/auth/authenticate`;
export const refreshAccessToken = `${server}/auth/refresh`;
export const getAllEvents = `${server}/events`;
