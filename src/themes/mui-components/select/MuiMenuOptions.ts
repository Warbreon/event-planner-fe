import { COLORS } from '../../styles/Colors';

const muiMenuOptions = {
	styleOverrides: {
		paper: {
			marginTop: '0.2rem',
			borderRadius: '8px',
			boxShadow: `0.5px 0.5px 10px 0.5px ${COLORS.GRAY_BORDER}`,
		},
	},
};

export default muiMenuOptions;
